
INSTALL:

	To build the executable file simply type:

	$ make


	To install system-wide copy "fldev" to a 
	directory included in your PATH-Environment, 
	for example "/usr/local/bin".

	For instructions on patching fluid 
	read ./fluid-patch/README
